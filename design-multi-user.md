# 多使用者與各自獨立的 VFS 樹 — 設計方案

> **狀態：§6 三個決策點已拍板，§5 的第 1～3 步已於 2026-08-02 落地。** 2026-07-31 產出。
> 落地與方案的出入：§3.1 的 `owner_id` 欄位**沒有加**（前三步還不需要走訪整棵樹，
> 加一個必須跟 `parent_id` 保持一致的冗餘欄位是預先付代價）；`ROOT_ID` 也**沒有**改成
> `"root:<user_id>"`，既有的 `"root"` 直接指派給 env 帳號，因為目錄的 tag 蓋住自己的 id，
> 換 id 等於整棵樹重簽。**留在待辦的是第 4 步**，見 `ROADMAP.md`。
> **拍板結果：模型 B（每人一把 master key）／帳號存 `users` collection／照 §5 分四步走。**
> 理由寫在 `ROADMAP.md`「已拍板的長期決策」，不在這裡複述。
> **§5 的第 1～3 步已併進 `ROADMAP.md` 的 `[next] Client UI`**，會在 UI 的 API 層之前先跑掉；
> 留在 `[later]` 的多使用者是**第 4 步**，也就是真正開放第二個帳號的那一步。
> 讀本文件時請注意：**§2 與 §3.3 裡關於模型 A 的段落已經是死路，保留只為記錄當初的取捨。**

---

## 一、現況：單一使用者是「寫死」還是「剛好只有一個」

先把「要改的東西」講準，因為「加個 user 欄位」這種直覺低估了範圍。目前有四個地方
把「只有一個使用者」寫進了結構，不只是資料裡剛好只有一筆：

| 位置 | 現況 | 為什麼是結構性的 |
|---|---|---|
| `src/vfs.py` `ROOT_ID = "root"` | 模組級常數字串 | `ensure_root()`、`get_node()` 都直接用它當起點；樹只有一棵 |
| `src/sftp.py` `validate_password()` | 拿 `config.SFTP_USER` / `SFTP_PASSWORD` 做 `compare_digest` | 憑證來自 env，env 放不下 N 組 |
| `src/keystore.py` `KEYSTORE_ID = "master"` | 單一記錄 | **一把 master key 服務所有人** |
| `src/db.py` `_ensure_indexes()` | `(parent_id, filename)` unique | 這條剛好不用改，見 §3.1 |

第三列是整個設計的重點，其他三列都只是工程量。

---

## 二、威脅模型：先講清楚「隔離」要隔到哪

多使用者有兩種完全不同的產品，成本差一個數量級。**必須先選**，因為後面所有結構都由它決定。

### 模型 A：只隔離「看得到什麼」（共用金鑰）

每人一棵樹，但所有樹共用同一把 master key。

- SFTP 層面 A 看不到 B 的檔案（路徑解析進不去別人的樹）。
- **但 A 的密碼可以解開 B 的資料**——只要 A 拿得到 DB 的內容（備份、Mongo 直連、
  或任何一條讀得到 `nodes` 的路）。因為 A 的密碼解得開共用的 master key，
  而 B 的 chunk 是同一把 key 加密的。
- 適合的情境：同一個人的多個裝置/帳號，或彼此互相信任的小團隊。
- 成本：低。keystore 完全不動。

### 模型 B：真正的密碼學隔離（每人一把 master key）

- `keystore` 從一份記錄變成每人一份。
- A 的密碼在密碼學上就解不開 B 的 chunk，即使拿到整個資料庫。
- **代價一：跨使用者分享會變成需要真正的金鑰交換**（把 B 的檔案分享給 A，
  等於要用 A 的 KEK 再包一份檔案金鑰）。目前的架構裡沒有「每個檔案一把金鑰」這層，
  所以分享功能在模型 B 下不是加個權限欄位就能做的。
- **代價二：忘記密碼＝那個使用者的資料真的救不回來**。目前單一使用者有
  `SFTP_PASSWORD_OLD` 這條救援路，多使用者下每個人都要有等價機制，否則客服成本很高。
- 成本：高，而且不可逆——選了 A 之後要改成 B，需要把每個使用者的資料重新加密一次。

> **這份文件後面的結構同時支援兩者**，差別集中在 §3.3。但選擇必須先做，因為
> 「先做 A 之後再升級到 B」的遷移成本遠高於一開始就做 B。

---

## 三、結構設計

### 3.1 節點樹：每個使用者一個 root

**做法**：`ROOT_ID` 從常數變成「每個使用者一筆 `is_dir` 的節點，`parent_id` 為 `None`」。

```
nodes:
  id         "root:<user_id>"   # 或另一個 uuid，見下方取捨
  parent_id  None
  filename   ""
  owner_id   <user_id>          # 新欄位
  is_dir     true
```

**`owner_id` 要不要加在每一個節點上**：

- **加**（建議）：`children()` / 對帳腳本 / 刪帳號時的清理都能直接用 `owner_id` 查，
  不必從 root 往下走整棵樹。代價是每次 insert 多一個欄位，而且**它會變成一個
  必須跟 `parent_id` 保持一致的冗餘欄位**——不一致時以哪個為準要寫死在文件裡
  （建議：`parent_id` 是唯一真相，`owner_id` 只是查詢加速，任何走訪都不得只信它）。
- **不加**：沒有一致性問題，但「這個使用者用了多少空間」「刪帳號要刪哪些節點」
  都要走訪整棵樹。

**`(parent_id, filename)` unique 索引不用改**：不同使用者的 root 是不同的 `parent_id`，
所以 `/foo.txt` 在兩棵樹下天然就是兩筆不同的鍵。這是少數不用動的地方。

**`ensure_root()` 的時機要改**：現在它在啟動時跑一次（`main.py`，還沒有人認證）。
多使用者下 root 要在「使用者第一次成功登入後」建立，或在建立帳號時建立。
建議後者——啟動時建立需要列舉所有使用者，而登入時建立會讓
「登入」這條路徑帶上寫入副作用。

### 3.2 認證：憑證從 env 搬到資料庫

`validate_password()` 現在做兩件事（比對憑證、開 master key），多使用者下變成三件：

1. 用 `username` 查出使用者記錄（找不到時**仍要跑一次假的密碼驗證**，否則
   「帳號不存在」與「密碼錯」的耗時差異就是一個使用者列舉的洩漏管道）；
2. 驗證密碼雜湊；
3. 開這個使用者的 master key。

**密碼儲存**：用 Argon2id 的 `PasswordHasher`（`argon2-cffi` 已經是相依，本輪加的），
**不要**重用 `derive_kek`——那是導出金鑰，不是驗證密碼，兩者的輸出用途不同，
混用會讓「儲存的雜湊」與「金鑰」有共同的推導輸入。

```
users:
  username      str    unique index
  password_hash str    argon2id PHC 字串（自帶參數，與 keystore 記錄同樣的自描述原則）
  root_id       str
  created_at    int
  disabled      bool
```

**注意一個現有行為會變**：目前 `validate_password` 的兩次 `compare_digest` 是常數時間的。
換成資料庫查詢後，「帳號存在與否」本身就會有時間差，必須靠上面第 1 點的假驗證補平。
這是新的、目前不存在的攻擊面。

### 3.3 金鑰：`keystore` 的改法（模型 A / B 的分歧點）

**模型 A（共用金鑰）**：`keystore` 完全不動，`KEYSTORE_ID = "master"` 保留。
`open_master_key(password)` 的語意變成「用這個人的密碼開共用的 key」——
但這需要共用 key 被**每個使用者的密碼各包裝一份**，也就是 `keystore` 變成
每人一筆記錄、包的是同一把 key。所以其實 A 也要改 `keystore`，只是改得比較淺：

```
keystore:
  id        "user:<user_id>"     # 取代固定的 "master"
  ...       wrap_master_key() 產生的既有欄位，一個都不變
```

新增使用者＝把共用 key 用新密碼再包一份。**這意味著建立帳號的當下必須有人
持有共用 key**（例如管理員的連線正開著），這是模型 A 一個不明顯但很重要的操作限制。

**模型 B（每人一把）**：同樣的 `keystore` 結構，但每筆包的是**不同的隨機 key**。
建立帳號時 `generate_master_key()` 產一把新的，不需要任何既有金鑰在場。
結構上比 A 還單純——A 的複雜度在「建帳號要拿得到共用 key」。

> 兩個模型的 schema 幾乎一樣，差別只在「包的是同一把還是不同把」。
> 好消息是 `wrap_master_key()` / `unwrap_master_key()` 一行都不用改。

### 3.4 連線層

`DiscordVFS.__init__` 目前收 `key`，要再收 `root_id`。
`session_key` 這個 `extra_info` 要變成同時帶 key 與 root_id 的一個小物件。
`_node_versions`（跨 handle 同步用的 process 內字典）**不用改**——它以 node id 為鍵，
node id 全域唯一，不同使用者的節點天然不會撞。

### 3.5 Discord 那一側完全不動

所有使用者的 chunk 仍然進同一個 DM / 頻道。這是刻意的：
Discord 那側沒有「誰的」這個概念，隔離是靠加密與 metadata 做的，不是靠儲存位置。
**但要寫進文件**：模型 A 之下，能讀該頻道又拿得到 DB 的人可以讀所有人的檔案；
模型 B 之下不行。

---

## 四、會被順帶掀出來的題目（不解決會變成半成品）

這些不是「以後再說」，是**做多使用者的當下就會被問到**的：

1. **配額**。共用一個 Discord 帳號就是共用該帳號的儲存與速率上限。目前
   `DISCORD_MAX_CONCURRENCY` 是全域的——一個使用者灌大檔案會讓其他人全部變慢。
   最低限度要有 per-user 的並發配額，否則多使用者等於多了一個互相 DoS 的管道。
2. **刪帳號**。要刪 Discord 上的附件、刪節點、刪 keystore 記錄。目前
   `remove()` 一次刪一個檔案並逐一 `_safe_delete_message`；批次刪一整棵樹會撞 429，
   需要走既有的 rate limit 路徑並且可中斷續跑。
3. **分享**。模型 B 下這是真正的密碼學問題（見 §2），模型 A 下只是權限欄位。
   建議第一版明確**不做分享**，並在文件寫死。
4. **管理介面**。建帳號/改密碼/停用目前都沒有入口。SFTP 協定裡沒有地方掛這些，
   所以要嘛一支 CLI（`python -m src.admin`），要嘛靠環境變數在啟動時同步一份使用者清單。
   建議 CLI，因為 env 同步會讓「刪掉一行就等於刪帳號」這種危險操作太容易發生。

---

## 五、實作順序

每一步都能單獨驗證、單獨 commit。**前三步已於 2026-08-02 落地**：

1. ~~**`users` collection 與密碼雜湊**~~ **已完成**（`src/users.py`）。啟動時把 env 的
   `SFTP_USER` / `SFTP_PASSWORD` 同步進 `users`，`validate_password` 改成查資料庫。
   行為完全不變，是純粹的結構搬移。§3.2 講的「假驗證」已實作，**帳號停用也走同一條**
   ——否則「已停用」與「密碼錯」之間又是一個時間差。
2. ~~**`keystore` 的 id 改成 per-user**~~ **已完成**。`keystore.adopt_legacy_record()`
   在啟動時把既有的 `"master"` 改名到帳號 id 底下。**它只改 `id` 一個欄位**：密文、salt、
   nonce、MAC、KDF 參數原樣搬過去，所以不需要密碼，也不可能讓記錄變得打不開。
   而且它**刻意不以密碼能否解開為條件**——同一次重啟裡又改了密碼的話，
   有條件的版本會把記錄留在舊 id 底下，然後報一個兩個原因都沒提到的錯。
3. ~~**root 從常數變成 per-user**~~ **已完成**。`DiscordVFS(key, root_id)`，`root_id`
   沒有預設值。既有的 `"root"` 節點直接指派給既有那個使用者，所以一個 tag 都不用重算。
4. **開放第二個使用者**（待辦）：管理 CLI、建帳號流程、per-user 配額。
   **這一步之前要先有密碼救援路徑**——模型 B 之下忘記密碼等於那個人的資料真的沒了。

前三步跑完，系統仍然是單一使用者、行為不變，但結構已經是多使用者的了。
**這很重要**：如果做到一半決定不做了，前三步是可以留下來的，不是白工。

---

## 六、三個決策點（2026-08-02 全部拍板）

1. **模型 A 還是模型 B** → **B，每人一把 master key**。
   關鍵是不可逆性：A 改 B 要把所有人的資料重新加密，而現在線上幾乎沒有資料，
   是成本最低的時機。**連帶結論：分享明確不做**（§4.3），因為現有架構沒有
   「每個檔案一把金鑰」這層；**開放第二個帳號前必須先有密碼救援路徑**（§2 代價二）。
2. **帳號存哪** → **`users` collection**。設定檔會讓「刪掉一行就等於刪掉一個人的
   全部資料」太容易發生。代價是需要一支管理 CLI，排在第 4 步。
3. **怎麼過渡** → **照 §5 分四步走**。理由是可診斷性：一次到位會同時動認證、金鑰
   與樹根，而其中兩層寫壞的後果是所有位元組永遠讀不出來，出事時必須分得出是哪一層。

**第 4 步之前不會有任何使用者能真的多開**，所以「要不要真的開放多使用者」是一個
獨立的、可以推遲到第 3 步跑完再做的決定——前三步不預先承諾它，這也是分四步的理由之一。
