# 多使用者與各自獨立的 VFS 樹 — 設計方案

> **狀態**：2026-07-31 產出；§6 三個決策點已拍板，§5 的第 1～3 步已於 2026-08-02 落地。
> **拍板結果：模型 B（每人一把 master key）／帳號存 `users` collection／照 §5 分四步走。**
> 理由在 `ROADMAP.md`「已拍板的長期決策」，不在這裡複述。
>
> **落地與方案的兩處出入**：§3.1 的 `owner_id` 欄位**沒有加**（前三步還不需要走訪整棵樹）；
> `ROOT_ID` 也**沒有**改成 `"root:<user_id>"`，既有的 `"root"` 直接指派給 env 帳號，
> 因為目錄的 tag 蓋住自己的 id，換 id 等於整棵樹重簽。
>
> **留在待辦的是第 4 步**，見 `ROADMAP.md`。
> **§2 與 §3.3 裡關於模型 A 的段落已經是死路**，保留只為記錄當初的取捨。

---

## 一、現況：單一使用者是「寫死」還是「剛好只有一個」

有四個地方把「只有一個使用者」寫進了結構，不只是資料裡剛好只有一筆：

| 位置 | 現況 | 為什麼是結構性的 |
|---|---|---|
| `src/vfs.py` `ROOT_ID = "root"` | 模組級常數字串 | `ensure_root()`、`get_node()` 都直接用它當起點；樹只有一棵 |
| `src/sftp.py` `validate_password()` | 拿 `config.SFTP_USER` / `SFTP_PASSWORD` 做 `compare_digest` | 憑證來自 env，env 放不下 N 組 |
| `src/keystore.py` `KEYSTORE_ID = "master"` | 單一記錄 | **一把 master key 服務所有人** |
| `src/db.py` `_ensure_indexes()` | `(parent_id, filename)` unique | 這條不用改，見 §3.1 |

第三列是整個設計的重點，其他三列都只是工程量。

---

## 二、威脅模型：隔離要隔到哪

### 模型 A：只隔離「看得到什麼」（共用金鑰）— 未採用

每人一棵樹，所有樹共用同一把 master key。SFTP 層面 A 看不到 B 的檔案，**但 A 的密碼可以
解開 B 的資料**——只要拿得到 DB 的內容。成本低，keystore 幾乎不動。

### 模型 B：真正的密碼學隔離（每人一把 master key）— 已採用

`keystore` 從一份記錄變成每人一份，A 的密碼在密碼學上就解不開 B 的 chunk。

- **代價一：跨使用者分享會變成需要真正的金鑰交換**。現有架構沒有「每個檔案一把金鑰」這層。
- **代價二：忘記密碼＝那個使用者的資料真的救不回來**。單一使用者有 `SFTP_PASSWORD_OLD`
  這條救援路，多使用者下每個人都要有等價機制。
- 選 B 而不是 A 的關鍵是不可逆性：A 改 B 要把每個使用者的資料重新加密一次。

---

## 三、結構設計

### 3.1 節點樹：每個使用者一個 root

`ROOT_ID` 從常數變成「每個使用者一筆 `is_dir`、`parent_id` 為 `None` 的節點」。

```
nodes:
  id         "root:<user_id>"   # 或另一個 uuid
  parent_id  None
  filename   ""
  owner_id   <user_id>          # 新欄位（實際未加）
  is_dir     true
```

**`owner_id` 的取捨**：加了之後 `children()`／對帳腳本／刪帳號都能直接查，不必從 root 走訪
整棵樹，代價是它變成一個必須跟 `parent_id` 保持一致的冗餘欄位（`parent_id` 是唯一真相，
`owner_id` 只是查詢加速，任何走訪都不得只信它）。不加則沒有一致性問題，但配額與刪帳號要走訪整棵樹。

**`(parent_id, filename)` unique 索引不用改**：不同使用者的 root 是不同的 `parent_id`，
所以 `/foo.txt` 在兩棵樹下天然就是兩筆不同的鍵。

**`ensure_root()` 的時機要改**：從啟動時改成建立帳號時。啟動時建立需要列舉所有使用者，
而登入時建立會讓「登入」這條路徑帶上寫入副作用。

### 3.2 認證：憑證從 env 搬到資料庫

`validate_password()` 從兩件事變成三件：

1. 用 `username` 查出使用者記錄（找不到時**仍要跑一次假的密碼驗證**，否則「帳號不存在」與
   「密碼錯」的耗時差異就是一個使用者列舉的洩漏管道）；
2. 驗證密碼雜湊；
3. 開這個使用者的 master key。

**密碼儲存用 Argon2id 的 `PasswordHasher`，不要重用 `derive_kek`**——那是導出金鑰不是驗證密碼，
混用會讓「儲存的雜湊」與「金鑰」有共同的推導輸入。

```
users:
  username      str    unique index
  password_hash str    argon2id PHC 字串（自帶參數）
  root_id       str
  created_at    int
  disabled      bool
```

**一個現有行為會變**：目前 `validate_password` 的兩次 `compare_digest` 是常數時間的，
換成資料庫查詢後「帳號存在與否」本身就有時間差，必須靠第 1 點的假驗證補平。這是新的攻擊面。

### 3.3 金鑰：`keystore` 的改法（兩個模型的分歧點）

兩個模型的 schema 幾乎一樣，`wrap_master_key()` / `unwrap_master_key()` 一行都不用改：

```
keystore:
  id        "user:<user_id>"     # 取代固定的 "master"
  ...       wrap_master_key() 產生的既有欄位，一個都不變
```

差別只在「包的是同一把還是不同把」。**模型 A**（未採用）每筆包的是同一把共用 key，
所以建立帳號的當下必須有人持有共用 key——這是 A 一個不明顯但很重要的操作限制。
**模型 B** 每筆包不同的隨機 key，建帳號時 `generate_master_key()` 產一把新的即可。

### 3.4 連線層

`DiscordVFS.__init__` 要再收 `root_id`，`session_key` 這個 `extra_info` 變成同時帶 key 與
root_id 的物件。`_node_versions` **不用改**——它以全域唯一的 node id 為鍵。

### 3.5 Discord 那一側完全不動

所有使用者的 chunk 仍然進同一個 DM / 頻道。Discord 那側沒有「誰的」這個概念，
隔離靠加密與 metadata，不靠儲存位置。模型 A 之下能讀該頻道又拿得到 DB 的人可以讀所有人的檔案；
模型 B 之下不行。

---

## 四、會被順帶掀出來的題目

1. **配額**。共用一個 Discord 帳號就是共用該帳號的儲存與速率上限，而
   `DISCORD_MAX_CONCURRENCY` 是全域的。最低限度要有 per-user 的並發配額，
   否則多使用者等於多了一個互相 DoS 的管道。
2. **刪帳號**。要刪 Discord 附件、節點、keystore 記錄。`remove()` 一次刪一個檔案並逐一
   `_safe_delete_message`，批次刪整棵樹會撞 429，需要走既有 rate limit 路徑且可中斷續跑。
3. **分享**。模型 B 下這是真正的密碼學問題。**第一版明確不做。**
4. **管理介面**。建帳號／改密碼／停用目前都沒有入口，SFTP 協定裡也掛不上。
   選 CLI（`python -m src.admin`）而不是「env 同步一份使用者清單」，因為後者會讓
   「刪掉一行就等於刪帳號」太容易發生。

---

## 五、實作順序

每一步都能單獨驗證、單獨 commit。**前三步已於 2026-08-02 落地**：

1. ~~**`users` collection 與密碼雜湊**~~ **已完成**（`src/users.py`）。啟動時把 env 的
   `SFTP_USER` / `SFTP_PASSWORD` 同步進 `users`，`validate_password` 改成查資料庫。
   行為完全不變。§3.2 的假驗證已實作，**帳號停用也走同一條**——否則「已停用」與「密碼錯」
   之間又是一個時間差。
2. ~~**`keystore` 的 id 改成 per-user**~~ **已完成**。`adopt_legacy_record()` 在啟動時把既有的
   `"master"` 改名到帳號 id 底下，**只改 `id` 一個欄位**，所以不需要密碼，也不可能讓記錄
   變得打不開。它**刻意不以密碼能否解開為條件**——同一次重啟裡又改了密碼的話，
   有條件的版本會把記錄留在舊 id 底下，然後報一個兩個原因都沒提到的錯。
3. ~~**root 從常數變成 per-user**~~ **已完成**。`DiscordVFS(key, root_id)`，`root_id` 沒有預設值。
   既有的 `"root"` 節點直接指派給既有使用者，所以一個 tag 都不用重算。
4. **開放第二個使用者**（待辦）：管理 CLI、建帳號流程、per-user 配額。
   **這一步之前要先有密碼救援路徑。**

前三步跑完，系統仍然是單一使用者、行為不變，但結構已經是多使用者的了。
**做到一半決定不做的話，前三步可以留下來，不是白工。**

---

## 六、三個決策點（2026-08-02 全部拍板）

1. **模型 A 還是 B** → **B，每人一把 master key**。關鍵是不可逆性：現在線上幾乎沒有資料，
   是成本最低的時機。連帶結論：**分享明確不做**（§4.3）；**開放第二個帳號前必須先有
   密碼救援路徑**（§2 代價二）。
2. **帳號存哪** → **`users` collection**。設定檔會讓「刪掉一行就等於刪掉一個人的全部資料」
   太容易發生。代價是需要一支管理 CLI，排在第 4 步。
3. **怎麼過渡** → **照 §5 分四步走**。理由是可診斷性：一次到位會同時動認證、金鑰與樹根，
   其中兩層寫壞的後果是所有位元組永遠讀不出來，出事時必須分得出是哪一層。

**第 4 步之前不會有任何使用者能真的多開**，所以「要不要真的開放多使用者」是一個獨立的、
可以推遲到第 3 步跑完再做的決定。
