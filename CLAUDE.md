# 決策分級

## 可自動執行（唯讀/低風險，不必回報即可做）
- 讀檔、grep、git status / log / diff
- 型別檢查、lint、跑既有測試
- 讀 README、package.json、既有文件

## 先產出方案，經確認才動工（中風險）
- 技術方案、檔案修改計畫
- 架構/資料流調整設計
- 風險與影響範圍列表

## 必須先問我，逐項確認（高風險，未經同意不可執行）
- 改 schema / 建 migration / db push
- push main / deploy / 改 production env
- 刪除或覆蓋既有資料
- 改權限模型 / 批次改用戶資料

> 不確定屬於哪一層，一律視為高風險，停下來問。
> 這份規則是行為引導，不是強制層；高風險項目的實際攔阻待補 PreToolUse hook。

## 臨時例外：重啟 docker 服務不必先問（2026-07-31 起）

**可以隨時重啟 docker 服務**（`docker compose up -d`、`up -d --build`、`restart`），
不需要事先徵求同意。理由：專案尚未正式上線使用，沒有人在用這個服務，
重啟的代價只有幾秒鐘的不可用。

這條**只涵蓋重啟與重建**。以下仍然是高風險、仍然要先問：
`docker compose down -v`（會刪 volume）、改 production env、刪除或覆蓋既有資料。

> **這是一條有到期日的規則。等我說「專案已上線使用」，就把整個這一節刪掉**，
> 屆時重啟會重新回到「必須先問」那一層。

---

# 檔案讀取限制

`BLUEPRINT.md` 僅供使用者本人查閱。除非使用者明確要求（例如要求討論架構、
要求執行 `/blueprint` 重新產出），否則不要主動讀取這份檔案——一般探索、
規劃、委派 subagent 時都不需要先掃過它。

---

# 提問機制

## 一定要問我的情況

不要自己猜、不要「先做了再說」、不要用預設值蒙混過去：

- 命中上面「高風險」分級的任何一項
- 兩種以上做法會導向**不同的架構或資料結構**，且不是單純風格差異
- 需求有兩種讀法，且不同讀法產出的東西**實質不同**
- 要偏離既有規格 / 既有文件（如 `ROADMAP.md`「已拍板的長期決策」、`missing_info.md`）寫定的決策
- 動到加密演算法、金鑰處理、認證流程
- 要新增或移除一個相依套件
- 發現任務前提本身有問題（例如「照著做會壞掉」）——先講，再問要不要換路

## 不要問我的情況

這些自己決定，問了只是浪費一輪：

- 命名、格式、檔案擺放位置、註解密度
- 有明顯慣例或唯一合理解的選擇
- 唯讀類操作（讀檔、grep、跑既有測試、型別檢查）
- 我已經在本輪或前一輪明確回答過的同一件事

## 怎麼問

1. **一次問完**——把整批決策點湊齊再問，不要一題一輪來回消耗我。
2. **給選項不給開放題**——每個選項附「這樣選會怎樣」，並標出你的推薦及理由。
3. **講清楚代價**——如果不問直接做，最壞會發生什麼；以及事後要改回來有多貴。
4. **問完就停**——高風險項在我回答前不得動工。中風險項可以先做不受該決策影響的部分。

## 問完之後

- 我的回答若牽涉往後都適用的決策，寫進 `SOP.md` 或 `ROADMAP.md`，不要下一輪又問一次。
- 我如果重申或堅持原本的要求，就照做，不要再勸第二次。

---

# 主 Agent 委派 Subagent 的結構化邊界

委派前 prompt 必須包含以下五項，缺一不可：

1. **背景**：相關檔案/現有架構，不要讓 subagent 重新猜測
2. **目標**：這次要解決什麼
3. **可改範圍**：允許動的檔案/模組
4. **不可改範圍（Non-scope）**：明確列出不能碰的檔案、schema、API 介面、外部服務
5. **驗證方式**：完成後如何自證（type check / build / 測試通過），未驗證不可回報「已完成」

Subagent 回報固定用這個結構：
`完成項 / 未完成項 / 邊界衝突（發現需求超出可改範圍時，停止回報，不擅自擴權）`

Subagent 必須總是使用 Sonnet 模型，不論主模型種類

---

# 累積型檔案觸發規則

- 遇到重複性問題先讀 `SOP.md`；同一類問題第二次出現時寫入
- 新想法寫入 `ROADMAP.md` 並標記 `[now]` / `[next]` / `[later]` / `[parked]`，不當場插隊
- 跨多輪任務結束前依 `.claude/templates/session-handoff.md` 產出交接摘要

---

# Git Commit 規範

- Commit 訊息一律使用英文
- 作者和電子郵件欄必須固定填入使用者的 git config name/email
