# 決策分級

## 可自動執行（唯讀/低風險，不必回報即可做）
- 讀檔、grep、git status / log / diff
- 型別檢查、lint、跑既有測試
- 讀 README、package.json、既有文件

## 動工前留痕，不等確認（中風險）
- 技術方案、檔案修改計畫
- 架構/資料流調整設計
- 風險與影響範圍列表

方案寫進當輪工作區就開工，不停下來等我點頭。要推翻既有已拍板決策才升為高風險。

## 必須先問我，逐項確認（高風險，未經同意不可執行）
- **改資料格式**——三段式判定，依序問，第一個命中的就是答案：

  | 問 | 答 | 分級 |
  |---|---|---|
  | 1. 這次改動之後，**既有的存檔還讀不讀得起來**？ | 讀不起來 | **高風險**。即使一行 SQL 都沒跑、即使你只是寫了程式碼還沒執行 |
  | 2. 讀得起來。那**既有進入點寫出來的東西，位元組變了嗎**？ | 變了 | **中風險**。動工前留痕，並在 `QUESTIONS.md` 記一行：改了什麼形狀、為什麼、回退成本 |
  | 3. 兩者都沒變（純新增選配欄位，且不帶該欄位時輸出與從前逐位元組相同） | | 低風險，直接做 |

  驗第 2 題不要用眼睛看：拿改動前後的版本各跑一次同樣的寫入指令，`cmp` 兩個輸出檔。

- 建 migration / db push
- deploy / 改 production env
- 刪除或覆蓋既有資料
- 改權限模型 / 批次改用戶資料
- 新增或移除相依套件
- 動加密演算法 / 金鑰處理 / 認證流程
- 委派 subagent —— prompt 必須含〈委派邊界規格〉五項，缺一不可。
  一次開多個受託方另外還要先報方案等同意

> 不確定某個動作是否不可逆、或是否影響 `main`／production／既有資料時，視為高風險。
> 其餘不確定照「長時工作模式」的判準走：正確做法不會因未決問題而改變，就去做。
> 這份規則是行為引導。唯一有強制層的是 push：`.claude/hooks/block-push-main.py`
> 會擋掉目標為 `main` 或 `master` 的 push，不論寫成 refspec 還是靠當前分支。
> 其餘高風險項仍然只有這份文件擋著。

---

# 檔案讀取限制

`BLUEPRINT.md` 僅供使用者本人查閱。除非使用者明確要求（例如要求討論架構、
要求執行 `/blueprint` 重新產出），否則不要主動讀取這份檔案——一般探索、
規劃、委派 subagent 時都不需要先掃過它。

---

# 提問機制

## 開工前：先把設計軸列出來

**動手寫程式之前，列出這個任務有幾個互相獨立的設計軸**，每軸寫下至少兩個具體選項。

判準不是「A 感覺很明顯」，而是：**你能不能面不改色地寫出一個選項 B，
並說出哪種人會選它。**寫得出來就不是明顯慣例，該軸進 `QUESTIONS.md`。

例：「加上標籤功能」有三個軸——標籤存在物件裡還是獨立對照表（資料結構）、
建立時標還是事後標（介面）、多個標籤是 AND 還是 OR（查詢）。三個都寫得出選項 B。

**`QUESTIONS.md` 這一輪一定要被碰過。** 有題就開題；確認沒有待決問題，
就在〈待拍板〉寫一行「本輪無待決問題。已檢查的軸：X / Y / Z」。
檔案原封不動交出去，等於沒有人能分辨你是查過了還是根本沒想。

## 一定要問我的情況（寫進 `QUESTIONS.md`，不是當場中斷）

不要自己猜、不要「先做了再說」、不要用預設值蒙混過去：

- 命中上面「高風險」分級的任何一項
- 兩種以上做法會導向**不同的架構或資料結構**，且不是單純風格差異
- 需求有兩種讀法，且不同讀法產出的東西**實質不同**
- 要偏離既有規格 / 既有文件（如 `ROADMAP.md`「已拍板的長期決策」）寫定的決策
- 發現任務前提本身有問題（例如「照著做會壞掉」）——先講，再問要不要換路

## 不要問我的情況

這些自己決定，問了只是浪費一輪：

- 命名、格式、檔案擺放位置、註解密度
- 有明顯慣例或唯一合理解的選擇
- 唯讀類操作（讀檔、grep、跑既有測試、型別檢查）
- 我已經在本輪或前一輪明確回答過的同一件事

## 怎麼問

1. **一次問完**——把整批決策點湊齊再端上來，不要一題一輪來回消耗我。載體是 `QUESTIONS.md`。
2. **給選項不給開放題**——每個選項附「這樣選會怎樣」，並標出你的推薦及理由。
3. **講清楚代價**——如果不問直接做，最壞會發生什麼；以及事後要改回來有多貴。
4. **不要停下來等**——寫進 `QUESTIONS.md`，換去做不依賴這個答案的事，見「長時工作模式」。
   高風險項在我回答前一律不執行，但它需要的東西可以先備好。

## 問完之後

- 我的回答若牽涉往後都適用的決策，寫進 `SOP.md` 或 `ROADMAP.md`，不要下一輪又問一次。
- 我如果重申或堅持原本的要求，就照做，不要再勸第二次。

---

# 長時工作模式

預設行為，不必另外交代。一次委派可連續工作數小時，中途不回報進度。

「輪」＝一次委派。同一輪內的重試叫「次」。

## 開工前：先驗上一輪，再挑新工作

1. 跑一次上一輪宣稱完成的功能的驗證路徑。內層測試不算，要真依賴：
   `docker compose up -d` 看啟動 log、`pytest --db=sqlite`
2. 壞了就 `git revert` 那個 commit、在 `ROADMAP.md` 改回未完成。修它排在所有新工作前面；
   修 3 次仍紅就停在 revert 後的狀態，寫進 `QUESTIONS.md`，回去挑新工作
3. 讀 `DECISIONS.jsonl`（壓縮前由 hook 自動抽出的決策）。要推翻裡面的決策前先看一遍，
   不要重新論證已經論證過的事

交接摘要與 `git log` 打架時，信 `git log`。摘要是 agent 寫的散文，寫入可能被中斷；log 是 append-only。

## 開工前：釘住收斂條件

寫下這一輪的收斂條件——哪個指令輸出什麼數字或狀態才算完成，寫進當輪工作區。
卡住時不准把「完成」的定義改成配合已經寫出來的東西。

範圍不凍結：工作中長出來的目標當場做掉，見「衍生目標」。擋住你的是高風險層，不是範圍。

## 需要拍板的事：不停下來

1. 寫進 `QUESTIONS.md`
2. 受影響的部分留 stub，標 `BLOCKED(Q<編號>)`
3. 換做不依賴這個答案的工作
4. 再也找不到不受阻的工作時，才停下來，一次端上全部

判準：**這件事的正確做法會不會因未決問題而改變**。不會就去做。
同一個決策點只寫一題。既有的題又擋住一件新工作時，在那題底下補一行現況，不要開新題。

## 高風險項：不做，而不是停下來問

- 不執行、不繞過、不用等價的低風險做法偷渡
- 寫進 `QUESTIONS.md` 的「待你執行／待你批准的動作」
- 它需要的 migration、腳本、設定、測試全部備好但不執行
- 備好的測試不得讓測試總數變紅：缺套件的用 `pytest.importorskip` 包住，並在 `QUESTIONS.md` 記一行

## 衍生目標：當場做掉

工作中長出來的目標不進 `ROADMAP.md` 排隊，當場做完再往下走，直到再也長不出新目標。
兩個例外：命中高風險層的寫 `QUESTIONS.md`；與當前委派無關的新想法才進 `ROADMAP.md`。

## 結束這一輪的條件

- 收斂條件達成，且再也長不出衍生目標
- 剩下的全卡在 `QUESTIONS.md`
- 同一問題修 3 次仍紅 → 寫進「卡住」區，換下一件
- 連續兩次嘗試沒有產生新資訊
- 任務前提本身是錯的
- 下一件工作必須等前一個 PR 合併才做得下去

回報三件事：完成什麼（附驗證數字）、`QUESTIONS.md` 全文、未做的部分。

---

# 留痕與收尾

**觸發條件：這一輪動過任何檔案。**只讀不寫的工作（回答問題、查資料、跑既有測試）不套用。

**過程中**

- 驗證綠了就 commit，不要攢著。數小時的單一巨大 diff 無法審查，只能整包丟掉
- 驗證沒綠的東西不 commit：留在工作目錄，寫進 `QUESTIONS.md` 的「卡住」

**一輪結束前**

1. `ROADMAP.md`：變更紀錄補一筆（日期／做了什麼／測試數），更新 `[now]` / `[next]` 標記。
   測試數變了就同步改「本專案的驗證指令」那幾個數字——兩邊漂開時沒有任何東西會變紅
2. `QUESTIONS.md`：清掉已答的題，長期決策搬進 `ROADMAP.md` 或 `SOP.md`
3. `SOP.md`：補這輪踩到的重複性問題，沒踩到就不寫
4. 開功能分支、push 它、開 PR 進 `main`。互不依賴的 PR 連續開，不要停；
   只有下一件工作必須等這個 PR 合併才做得下去時，才停手等我。**絕不 push `main` 或 `master`**
   ——那是 `block-push-main.py` 會擋下來的事，不是靠自律。
   工作已經 commit 在 `main` 上才想起這條時：`git checkout -b <分支>` 再
   `git branch -f main origin/main`，不要用 `git reset --hard`

**怎麼 commit**

- 一律用 `git-commit` skill，不要自己拼 `git commit` 指令
- **不要問我怎麼拆**——驗證綠了就直接提交一次，拆法你自己決定

---

# 驗證：不接受目測

「看起來對」「跑起來看過了」不算。完成的定義是**有一段先前會失敗、現在會通過的程式**。

## 打在使用者實際接觸的那一層

內層測試（直接呼叫函式、直接打內部 API）對「內層對、但沒接到外層」完全免疫，而那是最常壞的地方。

| 專案型態 | 使用者接觸的那一層 |
|---|---|
| 網頁 | 真瀏覽器打真伺服器 |
| 桌面 | 驅動真視窗 |
| CLI | 子程序執行真執行檔，斷言 argv / stdout / exit code |
| 函式庫 | 從安裝後的套件 import 公開 API |
| 服務 | 走真網路層，含中介層與標頭 |

本專案是瀏覽器（`client/app`）與桌面視窗（`client/shell`），兩層都已建立，測試檔都放 `tests/`：

| 層 | 檔案 | 怎麼打 |
|---|---|---|
| 瀏覽器 | `test_ui_login.py` | 真瀏覽器 → 真 aiohttp → 真 VFS/真資料庫 → `fake_discord` |
| 桌面視窗 | `test_ui_shell.py` | 真 Electron 視窗（`--remote-debugging-port` + Playwright `connect_over_cdp`）→ 真 preload bridge → 真主行程 → 真 `discord-drive.exe` |

啟動與連線細節在 `tests/shell_support.py`。零新增相依：Playwright 已在 `requirements-dev.txt`，
Electron 已是 `client/shell` 的 devDependency。

## 規則

- 碰使用者可見行為 → 該層要有測試，且**改動前是紅的**
- 新測試把被測行為刻意改壞一次，確認變紅，再改回來
- **互動式手段（瀏覽器操作、截圖、手動看輸出）只能用來 debug 已失敗的測試**，不能當驗收
- 不用截圖像素比對當預設；斷言打在角色、文字、狀態
- commit 前跑受影響那一層的測試；開 PR 前跑完整「本專案的驗證指令」四條，附數字。
  只動文件、不動程式與測試的變更免跑，PR 說明寫明它動了哪些檔案
- 還沒實際跑過的數字一律標明是預期值
- **本機全綠對「上線平台會不會壞」沒有意見。** 相依套件常在匯入時依平台綁不同實作
  （本專案踩到的是 asyncssh 看 `os.SEEK_DATA` 決定 SFTP 稀疏檔案走哪條路），
  Windows 上那條分支根本不執行，於是測試對它對錯一律綠。升級相依、或改動被這種
  分支包住的程式碼時，**驗收的那一次是 Linux CI，不是這台機器**；
  補測試要直接打自己那一側的方法，繞路走真的用戶端在唯一能執行它的平台以外沒有牙齒
- **條件式 skip 在「一定要跑」的環境裡要改成 fail。** 缺件在開發機上 skip 是對的，
  在 CI 上代表 workflow 壞了——而 skip 不會讓 job 變紅，於是「這一層有覆蓋」會以全綠的
  形式活很久。作法見 `tests/shell_support.py` 的 `refuse_to_skip_in_ci`
- **看到綠不算數，要看數字。** CI job 通過但測試全 skip 跟沒跑一樣；每次改完 CI 都去 log
  裡確認那個數字真的變了
- **任何「沒問題／可以了／檢查過了」的結論必須附 `檔案:行號` 或指令輸出。**
  只有結論沒有證據的審查視為沒有發生，要重做

## 本專案的驗證指令

- `./venv/Scripts/python.exe -m pytest`（792 項；不要用裸 `python`，見 `SOP.md` 第一條）
- `cd client/shell && node --test`（23 項）
- `./venv/Scripts/python.exe -m pytest --db=sqlite`（假件換成真 SQLite 跑同一套）
- `docker compose up -d` 看啟動 log

**這些數字對應已提交樹。** 工作目錄裡未提交的測試不算數——寫進文件的數字必須是
`git clone` 之後跑得出來的那個，否則下一輪會拿工作目錄的數字去對已提交的樹，對不起來。

**先建 SPA，否則 10 項直接 fail。** `client/app/dist/` 在 `.gitignore`，`git clone` 之後不存在；
`tests/conftest.py` 的 `built_client` 對「不存在」與「比 `src` 舊」都 `pytest.fail`，
於是 `test_ui_login.py` 5 項 + `test_ui_language.py` 5 項全紅。補法：`cd client/app && npm run build`。
改完 `client/app/src` 沒重建也是同一個症狀。

**另外三個前置條件缺了會 skip 而不是 fail**，所以看到 774 不是壞掉：

| 缺什麼 | skip 幾項 | 補法 |
|---|---|---|
| `client/shell/node_modules`（Electron） | 18 | `cd client/shell && npm install` |
| `dist-standalone/discord-drive.exe` | 4 | `python -m PyInstaller discord-drive.spec --noconfirm --distpath dist-standalone --workpath build-standalone` |
| Linux 上沒有 `DISPLAY` | 18 | `xvfb-run -a` 包住 pytest |

`node --test` 的整合部分同樣對真的 `dist-standalone/discord-drive.exe` 跑，缺了會 skip。

---

# 撰寫文件

適用於所有你產出的文字：`CLAUDE.md`、`SOP.md`、`ROADMAP.md`、`QUESTIONS.md`、README、
交接摘要、commit message、給我的回報。

- 只寫當下要用到的資訊。背景、沿革、「為什麼變成這樣」除非會改變讀者的行動，否則刪掉
- 規則寫成祈使句，刪掉「建議」「可以考慮」「原則上」
- 一句話講一件事，刪掉「也就是說」「換句話說」「值得注意的是」與同義覆述
- 理由只在它能讓讀者推廣到未列出的情況時才寫，寫一行
- 能用表格或清單就不用段落
- 寫完重讀一次，刪掉「刪掉之後意思不變」的字

---

# 累積型檔案觸發規則

- **動工前先讀 `SOP.md`** 對照症狀；命中就照該條的檢查順序走，並說出用了哪一條。
  同一類問題第二次出現時寫入；環境／工具決定、必然重現的第一次就寫。
  若這條教訓是「以後行為要改」而不只是「怎麼查」，一併升格成 `CLAUDE.md` 的規則
- **規則要能退場**：`SOP.md` 每條註明日期與當時的工具／模型版本。工具鏈或模型換代時逐條問
  「這條還在幫忙嗎」，不再重現的移進「已退役」區並註明退役日期，不要留在檢查清單裡佔位。
  規則只增不減，等於每一輪都在為早就不存在的問題付稅
- `DECISIONS.jsonl` 由 `.claude/hooks/extract-decisions.py` 在壓縮前自動 append，只增不改。
  hook 抓不到的決策（在程式碼裡做掉、沒寫成散文的）自己補一行，格式相同
- 需要我拍板的事寫入 `QUESTIONS.md`，不中斷工作（見「長時工作模式」）
- `QUESTIONS.md` 被答完後清空；有長期效力的決策搬進 `ROADMAP.md` 或 `SOP.md`
- 與當前委派無關的新想法寫入 `ROADMAP.md` 並標記 `[now]` / `[next]` / `[later]` / `[parked]`。
  由當前工作長出來的衍生目標當場做掉，不排隊——見「長時工作模式／衍生目標」
- 跨多輪任務結束前依 `.claude/templates/session-handoff.md` 產出交接摘要

---

# 委派邊界規格

<!-- 2026-08-18 從 SOP.md〈已退役〉恢復，並升級為 collab-kit 現行版。
     委派本身仍是〈決策分級〉高風險：未經使用者要求不執行。
     本節規範的是「一旦獲准委派，prompt 必須長什麼樣」。 -->

委派前 prompt 必須包含以下五項，缺一不可：

1. **背景**：相關檔案/現有架構，不要讓受託方重新猜測
2. **目標**：這次要解決什麼
3. **可改範圍**：允許動的檔案/模組
4. **不可改範圍（Non-scope）**：明確列出不能碰的檔案、schema、API 介面、外部服務
5. **驗證方式**：完成後如何自證，未驗證不可回報「已完成」

受託方回報固定用這個結構：
`完成項 / 未完成項 / 邊界衝突（發現需求超出可改範圍時，停止回報，不擅自擴權）`

受託方碰到需要拍板的事，一律回報給主 agent 彙整進 `QUESTIONS.md`，
不自己決定也不直接問使用者。

受託方必須總是使用固定的較便宜模型（目前預設 sonnet），不論主模型種類
——委派的成本效益只在「主模型較貴、受託方較便宜」時成立。

**一次開多個受託方要先報備。** 平行 fan-out、批次背景任務、實驗矩陣：
先講清楚要開幾個、各做什麼、預估規模、失敗會留下什麼，等同意再開。
受託方自己再開受託方時，這條一樣適用。
〔實測〕額度耗盡不是「慢一點」——進行中的工作會全部中斷在半途，留下一地半成品。

**受託方回報「完成」不構成完成。** 主 agent 收到報告後必須自己跑 `git diff` 確認宣稱的
改動真的存在、自己跑一次驗證指令，才可以轉述。兩者不一致時以 diff 與指令輸出為準，
並在回報裡指出報告哪裡不實。

**委派寫測試：先給軸，收貨用注入驗，不看綠不綠。**

- prompt 附上 `git diff` 得到的**函式清單**，不要只說「為這個模組寫測試」。實測：
  不給軸的三個回合交出 212 支測試、4557 行、全綠，對三個既有缺陷殺傷率 **0**；
  同一個模型給了函式清單後，19 支測試全中。
- 收到測試後把被測行為**逐一改壞**，確認對應的那一支變紅。沒有變紅的那一支等於不存在。
  「它跑過測試而且全綠」不是驗收證據——實測那 212 支測試**執行到了**三行缺陷
  （`trace --count` 分別是 8～11 次、1～5 次、1～2 次），然後對它們什麼都沒斷言。
- 指定驅動層。不指定時它一律直接呼叫內部函式：四個回合 231 支測試，走真實協定堆疊的是 **0** 支。

**環境限制不是不做的理由。** 有些 harness 禁止未經要求呼叫 Agent 工具，
這時冷 context 的第二人複驗做不了——照〈驗證〉自己走一遍敵對清單，
並在回報裡寫明「缺的是獨立第二人那一層」。
